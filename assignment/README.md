# 课程作业模板

## 任务说明
- 完成 `src/` 中的函数，实现指定功能。
- 使用 `pytest` 通过测试用例。

## 提交要求
- 代码通过 `flake8` 基础检查。
- 所有 `tests/` 用例通过。

## 本地运行
```bash
pip install -r requirements.txt  # 如存在
pip install pytest flake8
pytest -q
flake8 .
```
